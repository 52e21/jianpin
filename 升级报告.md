# 简聘升级报告（15 步全部完成）

> 生成时间：2026-09-12
> 评测集：`backend/eval/eval_set_v1.jsonl`（200 条）
> 基线：`backend/eval/baseline.json`（第 3 步，升级前）
> 最终回归：`backend/eval/regression_final.json`
>
> **本报告覆盖第 1–10 步的效果数据，以及第 12–15 步（四个缺陷修复）的验证结果——见第七节"最终对比"。**
>
> 第 12–15 步均为缺陷修复/边界防护，**不改变规则链路指标**，因此第二节的指标表在全部 15 步完成后
> 依然成立；最终回归已确认逐条结论 200/200 一致。

---

## 一、一句话结论

**第 1–10 步改变的是"可观测性、成本、隔离与角色能力"，不是"效果"。**
效果指标（结论一致率 / 技能 P-R）**零变化**——因为这一批改动没有触碰规则抽取与判分逻辑。
真正会改善效果的四个修复（第 12–15 步）**还没做**。

**已拿到的确定性收益**：面试题节点的 **prompt token 降 63.7%**（长输入 -60% ~ -81%）、
**每次分析稳定产出 4 条节点级 Trace**、**反馈回流接口打通**、**缓存按租户/会话/角色三重隔离**。

---

## 二、指标对比

### A. 200 条规则链路（关 LLM，确定性）

| 指标 | 基线 | 现在 | 变化 |
|---|---|---|---|
| 结论一致率 | 61.0% (122/200) | 61.0% (122/200) | **=** |
| 技能 P / R / F1（全部技能） | 89.28% / 96.43% / 92.17% | 89.28% / 96.43% / 92.17% | **=** |
| 技能 P / R / F1（必须技能） | 73.15% / 96.74% / 82.38% | 73.15% / 96.74% / 82.38% | **=** |
| 平均 token | 0（关 LLM） | 0 | **=** |
| P50 / P95 延迟 | 0.2ms / 0.4ms | 0.3ms / 0.7ms | +0.1 / +0.3ms（亚毫秒噪声） |
| 低分短路率 | 18.0% | 18.0% | **=** |
| 异常数 | 0 | 0 | **=** |
| 缓存命中数 | 0 | 0 | **=**（验证"关缓存"仍生效） |
| **逐条结论** | — | — | **完全一致（200/200）** |

> ⚠️ **61% 不能当"效果"读**：关 LLM 模式下，落在 50–79 分的样本必然降级为"待定"。
> 拆开看：78 条不一致里，**31 条是该模式的必然结果**，**47 条是真实缺陷**（见第五节）。

### B. 上下文结构化改造 A/B 实测（同一份代码，`ctx_mode` 开关，5 例真实调 LLM）

| 用例 | 输入字数 | raw prompt | structured prompt | 降幅 | raw 耗时 | struct 耗时 |
|---|---|---|---|---|---|---|
| E048（超长 JD） | 2141 | 1461 | **345** | **-76.4%** | 8312ms | 6807ms |
| E049（超长简历） | 3513 | 2298 | **439** | **-80.9%** | 7241ms | 6703ms |
| E016（前端示例） | 725 | 596 | 457 | -23.3% | 7605ms | 9014ms |
| E001（短输入） | 53 | 269 | 269 | 0%（走 200 字阈值） | 6427ms | 6632ms |
| E004（短输入） | 45 | 263 | 263 | 0%（走 200 字阈值） | 6493ms | 7367ms |
| E021（低分短路） | 57 | 0 | 0 | 不调 LLM | — | — |
| **均值** | | **977.4** | **354.6** | **-63.7%** | 7216ms | 7305ms |

- **prompt token 降 63.7%（仅长输入口径 -60.2%）** ✅ 达标（要求 ≥50%）
- **单次调用总 token 降 27.5%**（1767.7 → 1281.5）——降幅被 completion 拖累：5 道题各带
  答案要点与评分标准，输出侧约 800–1200 token **不受 prompt 改造影响**
- **端到端调用耗时基本不变**（7216ms → 7305ms，+1.2%，属 LLM 波动）
- **相关性未下降**：必须技能覆盖 10/14 → 10/14、缺失技能覆盖 2/4 → 2/4；
  人工逐条比对：E049 仍追问支付回调/幂等/一致性，E048 更针对 JVM/GC/并发（**部分更好**）

### C. LLM 小样本基线（升级前，6 例，供后续对照）

| 指标 | 值 |
|---|---|
| 结论一致率 | 50.0% (3/6) |
| 平均 token | 1285.8 |
| P50 / P95 延迟 | 7462.9ms / 9250.9ms |
| 异常数 | 0 |

---

## 三、逐项改动清单

| 步 | 内容 | 主要文件 | 验收结果 |
|---|---|---|---|
| 1 | 建评测集 200 条 | `backend/eval/build_eval_set.py`、`eval_set_v1.jsonl` | 200 条、**无 PII 自动断言通过** |
| 2 | 跑批脚本 | `backend/eval/eval_runner.py` | 一条命令 0.7s 跑完并打印 5 项指标 |
| 3 | 存基线 | `backend/eval/baseline.json` | 已存；含逐条结果供后续 diff |
| 4 | 建 `trace_events` 表 | `backend/app/database.py` | DDL 按执行书原文；13 列 + 2 索引；插入验证通过 |
| 5 | 四节点埋点 | `backend/app/trace.py`（新增）、`agent.py`、`tools.py` | **三条路径均稳定 4 行**；含 raw_response（截 2000）、finish_reason、degraded |
| 6 | 上下文结构化注入 | `tools.py`（`extract_experiences`/`build_interview_context`）、`agent.py` | prompt token **-63.7%**；相关性无下降 |
| 7 | 反馈接口 | `main.py`、`database.py`、`eval/test_feedback_api.py` | TestClient 6 组断言 + **真实 HTTP 服务**验证 PASS |
| 8 | 前端三按钮 | `components/feedback-bar.tsx`（新增）、`routes/result.tsx` | typecheck + build 通过；契约三方对齐 |
| 9 | 缓存加租户前缀 | `agent.py`、`main.py`、`frontend/lib/api.ts`、`match.tsx` | 不同 tenant/session **不命中同一缓存**；5 请求 3 个 key |
| 10 | role 角色切换 | `agent.py`、`main.py`、`match.tsx`、`result.tsx` | 三档分数文案按角色变化；`type` 机器值不变；**role 计入缓存 key** |

**新增工程约定**：所有验证脚本均屏蔽 `save_history` 与 Trace 写入，**评测数据不污染业务库**
（数据库始终为 33 行历史 / Trace 表由使用产生）。

---

## 四、升级过程中新发现的真实问题

| # | 问题 | 证据 | 状态 |
|---|---|---|---|
| 1 | **缓存命中时就地改写缓存对象** | 我的验收脚本第一次调用读到 `cache_hit=True`（被第二次调用篡改）——同一对象引用 | **待修（第 14 步）** |
| 2 | 无 Key 时 `llm_calls` 被记成 1 | **164/200 条**出现 `llm_calls>0` 但 `tokens=0`；根因是 `agent.py` 哨兵判 `"（未配置"`（带全角括号），`tools.py` 实际返回 `"未配置 DEEPSEEK_API_KEY..."`（无括号） | **待你决策**（不在执行书 15 步内） |
| 3 | `total_w=0 → 匹配度 100% + 推荐` | 7 条用例拿到"100 分 + 推荐"（含真实历史数据"人力专员"） | **待你决策**（不在 15 步内） |
| 4 | 技能子串误报 | E026：简历只有 `JavaScript` 却命中 `Java` → 100 分 + 推荐；假阳性来源 Top：`数据分析`15 次（来自职位名"数据分析**师**"）、`go` 10 次（来自 `Django`/`MongoDB`） | **待你决策**（不在 15 步内） |
| 5 | `summarize_recommendation` 的 `<50` 分支在 analyze 链路是死代码 | 低分路径 Trace 只出 3 行 → `analyze_agent` 用内联规则直接给结论，未调用该工具（两处重复实现） | 已留痕观测；**是否合并待你决策** |
| 6 | 低分路径结论由内联规则给出，与工具内规则文案不同 | 同上 | 同上 |

> 说明：第 2–6 项**不在执行书 15 步范围内**，我未擅自修改，避免污染第 11 步的基线对比。

---

## 五、评测体系现状与局限（必须一起看）

### 样本构成

| 来源 | 条数 | 说明 |
|---|---|---|
| `history` | 12 | 从 `execution_history` 恢复的真实输入（去重后、PII 已脱敏） |
| `sample_file` | 3 | `hire-tests/g1~g3.json` |
| `frontend_sample` | 1 | 前端 `match.tsx` 示例 |
| `constructed` | 184 | **44 条人工设计边界用例 + 140 条模板生成** |

### 三个必须知道的局限

1. **真实样本只占 8%（16/200）**。硬约束：`execution_history.task` 上限 102 字符（设计上的摘要），
   **数据库不存 JD/简历原文**；33 行里还有 12 行是测试垃圾。所以"200 条真实任务"物理上做不到。
2. **93/200 条标了 `needs_human_review`**。标注口径是**机器辅助初标**，不是人工标注；
   其中 66 条是"部分命中→待定"这类最不确定的期望值。
3. **模板样本偏"规则友好"**（我构造的规范文本），真实 HR 的 JD 会更脏 → **指标偏乐观**。

### 78 条结论不一致的构成（关 LLM 口径）

| 分类 | 条数 | 性质 |
|---|---|---|
| A：分数落在 50–79，本该交给 LLM | 31 | 关 LLM 模式的必然结果，**不是缺陷** |
| B：由规则决定却判错 | **47** | 真实缺陷（其中 46 条我标了待人工复核） |

B 类中：**假推荐 32 条 > 假不推荐 15 条**——招聘场景里错推一个不匹配的人代价更高。

---

## 六、下一步

1. ~~**第 12–15 步**：修结论枚举反转、防空列表越界、缓存就地改写、前后端 `.doc` 契约~~ → **已完成，见第七节**
2. ~~**最终回归**：追加"最终对比"章节~~ → **已完成，见第七节**
3. **待你决策的 4 项**（第四节 #2–#5）：是否纳入本次升级
   - `llm_calls` 计数 bug（无 Key 时误计 1 次，164/200 条）
   - `total_w=0 → 100 分 + 推荐`（7 条用例）
   - 技能子串误报（`JavaScript`→`Java`、`Django`→`go`、职位名"数据分析**师**"→幻影技能）
   - `summarize_recommendation` 的 `<50` 分支是死代码（两处重复实现）
4. **评测集迭代**：把 93 条待复核样本人工确认；逐步用真实使用数据替换模板样本
5. **人机协同闭环**：目前反馈已能落库，但"改判数据自动回流成评测集样本"还没做（现在是手动）

---

## 七、最终对比（第 12–15 步完成后）

### 指标：确认无回归

| 指标 | 基线（升级前） | 最终 | 说明 |
|---|---|---|---|
| 结论一致率 | 61.0% (122/200) | 61.0% (122/200) | **逐条 200/200 完全一致** |
| 技能 P / R / F1（全部技能） | 89.28% / 96.43% / 92.17% | 同 | 未改动抽取逻辑 |
| 技能 P / R / F1（必须技能） | 73.15% / 96.74% / 82.38% | 同 | 未改动抽取逻辑 |
| 低分短路率 / 异常数 / 缓存命中数 | 18.0% / 0 / 0 | 同 | — |
| P50 / P95 延迟（纯规则） | 0.2ms / 0.4ms | **0.4–0.7ms / 0.8–1.6ms** | 见下方说明 |
| 200 条跑批总耗时 | 0.7s | 0.7–0.9s | 基本不变 |

**关于延迟的诚实结论**：三次重复测量显示 P50 稳定在 0.4–0.7ms（基线 0.2ms），**这是一次真实但极小的开销**，
来源是：缓存 key 增加租户/会话/角色维度、payload 增加字段、`_traced_node` 包装、以及两处 `deepcopy`。
**在真实场景（1–2 次 LLM 调用，秒级）完全可忽略**；纯规则场景从 0.2ms 涨到 0.4ms 是可测量的，
需要知道但不值得优化。

### 为什么效果指标没变（重要）

第 12–15 步是**缺陷修复与边界防护**，不是效果优化：

| 步 | 修复内容 | 为什么不改变跑批指标 |
|---|---|---|
| 12 | 结论枚举解析反转（"不推荐"→"推荐"） | 只影响 LLM 分支（50–79 分区间），规则链路不经过；且模型在该区间极少输出"不推荐" |
| 13 | 空列表越界（模型返回空内容时 500） | 属于异常路径；正常样本永远不会走到 |
| 14 | 缓存命中串数据 | 只在同 key 重复请求时暴露；跑批每例前清缓存，天然不触发 |
| 15 | 前后端 `.doc` 契约不一致 | 纯前端/接口层，不进入跑批链路 |

**因此这四个修复必须靠单元测试验收——跑批测不出来。** 这也是本报告第七节的主要价值：
把"跑批看起来没变化"和"确实修好了"两件事分开证明。

### 第 12–15 步的验收证据

新增 4 个测试脚本，全部 `EXIT=0`（可独立运行，无需 pytest）：

| 脚本 | 覆盖 | 关键证据 |
|---|---|---|
| `eval/test_conclusion_parse.py` | 第 12 步 | 12 例单元测试 + **对照打印旧逻辑结果**（旧逻辑在 4/12 例上是错的）；端到端用假 LLM 注入"结论：不推荐" → 输出"不推荐" |
| `eval/test_empty_questions.py` | 第 13 步 | 假 LLM 返回空内容 → 底层函数**确实返回 `[]`**；`questions[0]` → IndexError（旧实现 500）；修复后 HTTP **200** + 占位题 |
| `eval/test_cache_isolation.py` | 第 14 步 | 旧写法最小复现（job_url 被覆盖、cache_hit 被篡改、`r1 is r2`）；当前实现 5 项断言全过，含"调用方篡改返回值不污染缓存"；SSE 链路确认无同类风险 |
| `eval/test_file_contract.py` | 第 15 步 | 用**行为**验证后端支持集合 `{pdf, docx}`；前端 accept/validTypes 与之双向一致；`.doc` 两端都给出可操作提示 |

**最终测试套件（5 个脚本）**：`test_conclusion_parse` / `test_empty_questions` / `test_cache_isolation`
/ `test_feedback_api` / `test_file_contract` —— **全部 EXIT=0**；`npm run typecheck` 通过。

---

## 八、《待办事项》第 1–3 项完成后的一致率变化（第二次基线）

> 基线文件：`backend/eval/baseline.json`（v1，升级前） → `backend/eval/baseline_v2.json`（v2，第 1–3 项完成后）
> 对比脚本：`backend/eval/compare_baselines.py`

### 8.1 总体指标

| 指标 | v1 基线 | v2 基线 | 变化 |
|---|---|---|---|
| **结论一致率（200 条）** | 61.0% (122/200) | **70.5% (141/200)** | **+9.5pt ← 首次变化** |
| 必须技能 P / R | 73.15% / 96.74% | **74.98% / 98.02%** | 提升 |
| 全部技能 P / R | 89.28% / 96.43% | **91.07% / 98.32%** | 提升 |
| 低分短路率 | 18.0% | 15.0% | 下降（更多用例进入可评估区间） |
| P50 / P95 延迟 | 0.2ms / 0.4ms | 0.7ms / 1.8ms | 归一化+词边界的成本，绝对值仍 <2ms |
| 异常数 | 0 | 0 | — |

### 8.2 分组指标（**两类口径必须分开看**）

| 分组 | 样本 | 结论一致率 | 必须技能 P / R |
|---|---|---|---|
| **业务一致性（人工设计边界）** | 44 | **79.55%** (35/44) | 90.17% / 92.95% |
| **业务一致性（真实输入/样本）** | 16 | **75.0%** (12/16) | 84.22% / 92.78% |
| **规范一致性（模板生成）** | 140 | 67.14% (94/140) | 69.76% / 100% |

**为什么要分开**：模板生成的 140 条，期望值天然由模板规则/规范阈值决定，**只能验证"实现是否符合自己的规范"**，
测不出"阈值定得对不对"；而且其中的期望值是从代码算出的分数推的，存在**循环论证风险**（见 8.3 的教训）。
把 140 条混进总一致率，会既虚高又掩盖真问题。业务判断类用例（60 条）才反映"判断对不对"。

### 8.3 24 条结论变化明细

| 分类 | 条数 | 说明 |
|---|---|---|
| **变好** | **21** | E012/E017/E018/E019/E026/E050（分母为 0 缺陷 + 子串误判）、E041/E054（否定窗口）、E063 等 13 条模板用例（否定窗口） |
| **变差** | 2 | E024（标注口径：MySQL 是否算 SQL）、E056（词表覆盖：Selenium/JMeter）——均为**已登记**的非缺陷项 |
| **仍不一致但结论变了** | 1 | E051（空 JD：`推荐→待定`，期望"不推荐"是本次复核建议改标的那条） |

**未登记回归：0 条。**

### 8.4 本次（第 1–3 项）修掉的真缺陷

| # | 缺陷 | 影响 | 修复 |
|---|---|---|---|
| 1 | `total_w=0` 给 100 分 + 推荐 | 7 条（含真实数据"人力专员"） | 改判「待定 + 建议人工复核」 |
| 2 | 技能子串误命中（`JavaScript`→`Java`、`Django`→`go`） | E026 等 | 词边界 + 写法归一化两层匹配 |
| 3 | JD 侧子串去重过激（`Java` 被 `JavaScript` 吞掉） | 影响 JD 技能抽取 | 改为"只有从不独立成词才算冗余" |
| 4 | **否定窗口跨小句**（`精通 A 与 B，不熟悉 C` 里 B 被误伤） | **15 条** | 窗口按标点截断（空格不算边界） |
| 5 | **缓存命中返回悬空 task_id**（点「采纳/改判」404） | 命中缓存的每次分析 | 命中缓存也生成新 task_id 并写自己的历史行；保留 `cache_source_task_id` 以便回溯。验收：`eval/test_feedback_after_cache.py` |
| 6 | **`llm_calls` 计数双向不一致** | 评测 v2 基线 **170/200 条**；真实库另有 **4 行** `tokens>0` 而 `llm_calls=0` | 不再用"返回文本是否以某前缀开头"猜，改由节点回报机器可读标志；不变量 **`tokens>0 ⟺ llm_calls>0`**。验收：`eval/test_llm_calls_count.py` |

> 缺陷 6 的连带修正：**「低分短路率」这个指标此前是失真的**（它等于"未进入面试题节点的比例"，
> 而不是"因低分主动跳过"）。修复后关 LLM 模式下它恒为 100%，跑批报告改为输出 `n/a` 并说明原因。

### 8.5 本次新登记、**未修**的规范/能力缺口（供后续排期）

1. **硬性门槛条件**：学历/年限门槛未满足或未知时仍可推荐（E031/E032/E034/E035，89–92 分照样推荐）
2. **上位概念蕴含**：MySQL→SQL、Spring Boot→Spring 这类"会下位即会上位"未支持（E024）
3. **语境强度**：`精通/熟练/熟悉/了解` 只用于区分必须/优先，**不参与命中强度**（E036）
4. **±25 字符语境窗口跨小句**：`精通 Java，熟悉 JavaScript` 把 JavaScript 判成"必须"（与缺陷 4 同类，方向不同，本次未动）
5. **词表扩容**：Selenium / JMeter 等（由业务定位决定，不因评测集加词）
7. **「必须技能缺 1 项是否仍推荐」——口径已拍定：短期维持允许缺 1 项**
   - **落地方式**：`app/tools.py` 的 `MAX_MISSING_REQUIRED_FOR_RECOMMEND = 1`
     （由隐含字面量改为**显式常量 + 决策注释**）
   - **闸门**：`eval/test_recommendation_policy.py` 锁住常量值与 9 个分界点行为 ——
     **改口径必须先改测试**，避免规范被无声改动
   - **依赖**：评测集里 21 条 `annotator=spec_derived` 的"推荐"依赖此口径
   - **长期**：「必须技能一项都不能缺」是产品决策，需单独立项评估，
     会连锁触发阈值调整 + 分数重算 + 基线重跑；**不为了评测集变绿而改规范**
6. ~~**缓存命中的 task_id 悬空**~~ → **已修复**（见 8.4 第 5 条）。
   附带行为变化：**命中缓存的请求现在也会产生一条历史记录**（标记 `cache_hit=1`，结果里注明"命中缓存，未重新调用 LLM"）。
   这是有意的：每次分析请求都应有自己的审计记录，否则既会 404、也让历史页漏记。

### 8.6 标注复核收尾与新基线（v4）

**① 21 条模板用例改标（`待定 → 推荐`）**

| 项 | 值 |
|---|---|
| `annotator` | **`spec_derived`**（由规范阈值推导，**不是** `human_reviewed`） |
| `review_status` | `spec_confirmed` |
| `label_basis` | `spec_threshold` |

> ⚠️ **口径依赖声明（必须连同数字一起看）**
> 这 21 条的"推荐"**依赖当前规范口径「缺 1 项必须技能仍可推荐」**：
> 需求文档 §5 写的是 `≥80 且缺失必须技能 ≤1 → 推荐`，规则明确允许缺 1 项。
> **这 21 条是规范口径驱动的，不是人工判断；规范若变，标注必须跟着变。**
> 如果业务口径改成"必须技能一项都不能缺"，这 21 条会回退到"待定"，
> 并触发阈值规则调整、分数重算与基线重跑 —— 属下一期事项，**不为了评测集变绿而改规范**。

**② E051（空 JD）改为接口契约用例**

原标"不推荐"无依据 —— 管线并未判定为不推荐，只是没有可评分维度。
空 JD 在**接口层**已由 `/api/agent/analyze` 拦截返回 **400**，这是**接口契约**；
而评测脚本直接调编排函数、绕过了接口校验，**等于用编排层的兜底行为去衡量接口层该负责的事，测错了层**。

处理：
- 新建 `backend/eval/contract_set_v1.jsonl`（6 条：空 JD→400、空简历→400、非法 role→400、
  反馈 task_id 不存在→404、改判缺 new_conclusion→400、action 非法→400）+ 执行器 `test_contract_set.py`
- 评测集里 E051 保留为**边界兜底记录**，标 `exclude_from_agreement: true`、`group: boundary`，
  `label_basis: boundary_behaviour`，**不计入结论一致率**

**③ 新基线 v4**（`baseline_v4.json`，评测集 `eval_set_v1_1.jsonl`）

| 指标 | v2 基线 | **v4 基线** | 说明 |
|---|---|---|---|
| 结论一致率 | 70.5% (141/200) | **81.4% (162/199)** | **分母 200 → 199**（E051 已剔除） |
| 业务一致性（人工设计） | 79.55% (35/44) | 81.4% (35/43) | 分母变化仅因 E051 属该组 |
| 业务一致性（真实输入/样本） | 75.0% (12/16) | 75.0% (12/16) | 未变 |
| 规范一致性（模板生成） | 67.14% (94/140) | **82.1% (115/140)** | 21 条改标生效 |
| 必须技能 P / R | 74.98% / 98.02% | 75.0% / 98.0% | 未变 |
| 异常数 | 0 | 0 | — |

**分母变化必须一起说明**：v2 的 141/200 与 v4 的 162/199 **不可直接比**。
同口径（都按 199 条、E051 不计）对比：v2 为 140/199 = 70.4%，v4 为 162/199 = 81.4%。

---


### 8.7 一条方法论教训（值得记下来）

复核 66 条模板用例时，我第一版结论是"34 条不一致全是我的标注错、代码零违规"——**这个结论一半是错的**。
因为那 34 条里有 13 条的期望值是我**用被 bug 压低的分数**推出来的"规范期望值"，
于是把 bug 编进了标注建议。直到发现否定窗口缺陷、重算修复后的分数，这 13 条才回到**原标**。

**教训：用实现产出（分数）去推导"标准答案"，会把实现的缺陷一起固化进评测集。**
凡是"规范一致性"类标注，必须同时标注 `label_basis`，并在实现变更后**重新推导**，不能一次定终身。

---

## 附 A：一键验证手册（交接用）

### A.1 环境

```powershell
cd C:\Users\admin\Desktop\2\agent-assistant\backend
$env:PYTHONIOENCODING="utf-8"     # 否则中文输出乱码
# 后端：.venv\Scripts\python.exe -m uvicorn app.main:app --port 8002
```

### A.2 跑全部测试（12 个脚本，无人值守）

```powershell
foreach ($t in @('test_recommendation_policy','test_contract_set','test_llm_calls_count',
                 'test_feedback_after_cache','test_negation_clause','test_skill_boundary',
                 'test_no_scorable','test_conclusion_parse','test_empty_questions',
                 'test_cache_isolation','test_feedback_api','test_file_contract')) {
    & ".venv\Scripts\python.exe" "eval\$t.py" > $null 2>&1
    "$t → EXIT=$LASTEXITCODE"
}
```

| 脚本 | 验证什么 |
|---|---|
| `test_recommendation_policy` | **推荐口径闸门**：`MAX_MISSING_REQUIRED_FOR_RECOMMEND=1` + 9 个分界点行为（改口径必须先改它） |
| `test_contract_set` | **接口契约**（6 条：空 JD/空简历/非法 role → 400；反馈 404/400）+ 编排层空 JD 不崩 |
| `test_llm_calls_count` | `llm_calls` 计数：无 Key 0/0、正常 1–2 次且 tokens>0、短路 0/0；**不变量 `tokens>0 ⟺ llm_calls>0`** |
| `test_feedback_after_cache` | **点采纳不再 404**：缓存命中返回新 task_id + 写历史行 + 删原历史后反馈仍成功 |
| `test_negation_clause` | **否定窗口按小句截断**：邻居否定不误伤、真实否定仍生效、E041 场景 33→67 分 |
| `test_skill_boundary` | 词边界（JavaScript↛Java、Django↛go）+ 写法归一化 + 子串去重收紧 |
| `test_no_scorable` | **加权分母为 0** → 待定 + 人工复核（E012/E017 验收） |
| `test_conclusion_parse` | 结论枚举解析不反转（12 例 + 端到端假 LLM） |
| `test_empty_questions` | 模型返回空内容时不 500 |
| `test_cache_isolation` | 缓存命中不串数据、调用方改返回值不污染缓存 |
| `test_feedback_api` | 反馈接口 6 组断言 |
| `test_file_contract` | 前后端文件类型契约（`.doc` 不再放行） |

### A.3 跑批与基线

```powershell
# 默认跑 eval_set_v1_1.jsonl（含复核元数据），关 LLM，秒级
.venv\Scripts\python.exe eval\eval_runner.py --out eval\baseline_v4.json
# 真实调 LLM 的小样本
.venv\Scripts\python.exe eval\eval_runner.py --limit 6 --with-llm --out eval\baseline_llm_small.json
# 基线与基线对比（自动给出分组指标 + 变好/变差清单）
.venv\Scripts\python.exe eval\compare_baselines.py --old baseline_v2.json --new baseline_v4.json
```

| 文件 | 含义 |
|---|---|
| `eval_set_v1.jsonl` | **原始评测集（200 条，未改动，无 annotator 字段）** |
| `eval_set_v1_1.jsonl` | 含复核元数据（`annotator` / `label_basis` / `review_status` / `exclude_from_agreement`） |
| `contract_set_v1.jsonl` | 接口契约用例集（6 条） |
| `baseline.json` | v1 基线（升级前，61.0% / 200） |
| `baseline_v2.json` | v2 基线（第 1–3 项后，70.5% / 200） |
| `baseline_v4.json` | **v4 基线（标注收尾后，81.4% / 199，E051 剔除）** |
| `baseline_llm_small.json` | 6 条真实调 LLM 的基线 |

### A.4 复核工具链（可重跑）

```
build_review_sheet.py      → 生成 93 条复核工作表 review_sheet_v1.csv
review_batch1_templates.py → 批次 1（66 条模板）复核建议 → review_advice_v1.jsonl
review_batch2_manual.py    → 批次 2（27 条人工设计）逐条判定 → review_advice_v2.jsonl
list_relabels.py           → 打印 22 条待确认改标（E051 单列）
apply_review.py --confirm <ids>  → 写回 eval_set_v1_1.jsonl（annotator 三档语义见文件头）
```

### A.5 诊断/取证脚本

| 脚本 | 用途 |
|---|---|
| `diagnose_negation_window.py` | 量化"否定窗口跨小句"误伤（82 处字面未命中 → 47 正确 / 35 误伤） |
| `simulate_clause_window_fix.py` | 修复前模拟：一致率 63.0%→70.5%，15 条变化 |
| `diagnose_llm_calls_count.py` | 分解 170 条 `llm_calls>0 且 tokens=0`（单一成因） |
| `diagnose_feedback_after_cache.py` | 缓存 task_id 悬空的取证（修复后应报"已修复"） |
| `measure_ctx.py` | 第 6 步 prompt token A/B（raw vs structured，-63.7%） |

### A.6 注意事项

1. **所有评测/诊断脚本都屏蔽 `save_history` 与 Trace 写入**。
   （`diagnose_feedback_after_cache.py` 例外——它必须写历史行来取证，但结束时会清理）
2. **测试只能删除自己创建的行**（按 `task_id` / `trace_id` 精确删除）。
   本会话踩过这个坑：`test_no_scorable.py` 原先把 `trace_events` **整表清空**，
   把浏览器点击验证产生的 4 行 Trace 证据一起删掉了。已修复，并立此纪律。
3. **Chrome 在本沙箱下 `workspace-write` 模式无法启动**（mojo 命名管道被拒），
   需以 `danger-full-access` 运行；浏览器点击验证的完整步骤见 A.7。
4. 基线数字**必须带上分母**：`141/200`（v2）与 `162/199`（v4）不可直接比；
   同口径对比是 `140/199 = 70.4% → 162/199 = 81.4%`。
5. **数据库当前状态**：`history = 34 行`（33 行历史 + **1 行来自浏览器点击验证**，含 feedback）、
   `trace = 0 行`。如需清掉那行验证数据：
   `DELETE FROM execution_history WHERE task_id = 'tk_d6c22cceed9b'`
   （注意该行 `task` 字段含前端示例简历的姓名/手机号，属演示数据）

---

### A.7 浏览器点击验证（真实 Chrome，需放宽沙箱）

**沙箱结论**：Chrome 在本沙箱下 `workspace-write` 模式**无法启动**
（`FATAL: mojo platform_channel: 拒绝访问`，`--single-process` 也绕不开）——
**这是沙箱模式限制，不是绝对限制**：同模式下 esbuild 也是 EPERM，放宽权限后即可运行。
以 `danger-full-access` 运行后，Chrome 152 正常启动，点击验证**已完成**。

```powershell
# 1) 放宽权限构建前端，并把后端地址烘焙进去（否则会打到 frontend/.env 里的 cpolar 地址）
cd ...\frontend; $env:VITE_API_BASE_URL="http://127.0.0.1:8015"; npm run build
# 2) 起后端（8015）与静态服务（3016，用 dist/_serve.py）
# 3) 放宽权限启动 Chrome：
#    chrome --headless=new --remote-debugging-port=9222 --user-data-dir=<workspace>\backend\.tmp\e2e-profile
# 4) 跑点击脚本（需在 frontend 目录，puppeteer-core 在那里）
node e2e-click-verify.cjs
```

**实测结果**（证据：`eval/e2e_click_evidence.json`）：

```
落地页 title=简聘 → 点「开始匹配」→ /match → 点「填入示例」(textarea 318/420 字)
→ 点「开始匹配」→ POST /api/agent/analyze 200 → /result
→ 点「采纳」→ OPTIONS+POST /api/agent/feedback 200 → 页面出现「已记录：采纳」✓
```
落库结果（真实浏览器点击 + **真实 LLM 调用**）：
```
execution_history id=100 task_id=tk_d6c22cceed9b
  feedback={"action":"采纳","original_conclusion":"推荐"}  feedback_at=2026-09-12T21:41:04
  llm_calls=1 tokens=1644 耗时=7778ms
trace_events（同 task）：parse_jd 5ms / match_resume 1ms /
  generate_interview_questions 1644tok 7178ms / summarize_recommendation 0ms  → 4 行
```

---

## 附：指标口径说明（避免误读）

- **结论一致率**：`pred_conclusion == gt.conclusion` 的占比；关 LLM 模式下 50–79 分必然为"待定"，
  因此 **61% 是该模式的下限，不是真实效果**
- **技能准确率/召回率**：以"标注者阅读 JD/简历语义写出的技能集合"为 ground truth，
  **不是**跑 `parse_jd` 得到的结果（否则指标必然 100%）；区分"严格口径"与"宽松别名口径"
- **prompt token**：来自 LLM API 返回的 `prompt_tokens`，按节点精确扣除（stats 由两个 LLM 节点共享累加）
- **低分短路率**：`llm_calls == 0` 的占比（注意：受第四节 #2 的计数 bug 影响，**当前该指标偏低**）
